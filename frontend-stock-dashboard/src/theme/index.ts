const theme = {
    typography: {
        fontFamily: ['Lato', 'sans-serif'].join(','),
        label: {
            fontSize: '20px',
            lineHeight: '24px',
            fontWeight: 600,
        }
    }
}

export default theme;